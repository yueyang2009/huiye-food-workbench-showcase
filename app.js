const toast=(message)=>{const el=document.querySelector('.toast');el.textContent=message;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),1800)};
document.querySelector('#share').addEventListener('click',async()=>{try{await navigator.clipboard.writeText(location.href);toast('链接已复制，可直接发送到手机');}catch{toast('请复制浏览器地址栏链接')}});
document.querySelector('[data-jump="risks"]').addEventListener('click',()=>document.querySelector('#risks').scrollIntoView({behavior:'smooth'}));
document.querySelector('#toggle-risks').addEventListener('click',(e)=>{document.querySelector('.risk.hidden').classList.toggle('hidden');e.currentTarget.textContent=e.currentTarget.textContent==='展开全部'?'收起':'展开全部'});
document.querySelectorAll('[data-page]').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('nav button').forEach(x=>x.classList.remove('active'));const nav=[...document.querySelectorAll('nav button')].find(x=>x.dataset.page===btn.dataset.page);if(nav)nav.classList.add('active');toast(`“${btn.dataset.page}”为展示版交互入口`)}));
